[project]
name = "tinynet"
version = "0.1.0"
description = "A tiny autograd-based neural network library in pure NumPy"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[tool.pytest.ini_options]
testpaths = ["tests"]
